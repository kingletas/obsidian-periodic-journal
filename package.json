{
	"name": "obsidian-periodic-journal",
	"version": "1.1.0",
	"type": "module",
	"scripts": {
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"dev": "node esbuild.config.mjs",
		"test": "node tests/build.mjs && tsc -noEmit -skipLibCheck && node esbuild.config.mjs production && node tests/run.mjs"
	},
	"devDependencies": {
		"@types/node": "^22.15.17",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.25.5",
		"obsidian": "latest",
		"typescript": "^5.8.3"
	},
	"dependencies": {}
}
